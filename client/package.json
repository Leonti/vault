{
  "name": "vault-client",
  "version": "1.0.0",
  "description": "json-server --watch example.json   python -m SimpleHTTPServer 8000   pulp build --to dist/vault.js",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "clipboard": "^1.6.1",
    "text-encoding": "^0.6.4"
  }
}
